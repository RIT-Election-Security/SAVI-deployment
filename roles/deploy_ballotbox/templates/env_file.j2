# Ballotbox Configuration
{% for variable, value in config_env_variables.items() %}
{{ variable }}="{{ value }}"
{% endfor %}
{% for variable, value in secret_config_env_variables.items() %}
{{ variable }}="{{ value }}"
{% endfor %}
